[{
    "name": "transfer",
    "group": "transfer",
    "type": "backend",
    "level": 1,
    "parameters": [
        {
            "name": "receiver",
            "type": "string",
            "required": true,
            "description": "The destination address or contact name to which the crypto will be transferred."
        },
        {
            "name": "amount",
            "type": "float",
            "required": true,
            "description": "The amount of crypto to be transferred."
        },
        {
            "name": "token",
            "type": "enum",
            "enum": ["USDC", "USDT"],
            "default": "USDC",
            "required": false,
            "description": "The type of token to be transferred. Options include USDC and USDT."
        },
        {
            "name": "chain",
            "type": "enum",
            "enum": ["Base", "ETH", "BSC"],
            "status":"true",
            "required": false,
            "description": "The blockchain network on which the transfer will occur. Options include Base, Ethereum, and BSC."
        },
        {
            "name": "run_mode",
            "type": "enum",
            "enum": ["auto", "manual"],
            "status":"true",
            "required": false,
            "description": "Specifies the mode of operation for the system. The mode can be set to 'auto' for automatic operation or 'manual' for manual operation, depending on user preference or system configuration."
        }
    ],
    "description": "Transfers cryptocurrency to another wallet, specifying the destination address, amount, blockchain network, and token type."
}]