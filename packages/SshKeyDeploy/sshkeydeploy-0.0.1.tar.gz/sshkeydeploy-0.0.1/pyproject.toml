[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "SshKeyDeploy"
version = "0.0.1"
authors = [
  { name="Julio Zamora", email="julio17795@gmail.com" },
]
description = "A CLI tool to create and deploy SSH keys"
readme = "README.md"
dependencies = ["paramiko", "scp"]

[project.urls]
Homepage = "https://github.com/Juliyo/SshKeyDeploy"

# Specify the CLI entry point here
[project.scripts]
SshKeyDeploy = "SshKeyDeploy.cli:main"