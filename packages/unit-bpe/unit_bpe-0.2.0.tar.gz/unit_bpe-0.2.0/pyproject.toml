[project]
name = "unit-bpe"
version = "0.2.0"
description = "BPE tokenizer that operates on integer sequences"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
dev-dependencies = [
    "maturin>=1.7.1",
    "pip>=24.2",
    "pytest>=8.3.2",
]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
