%SETS
RD = 0..1;
JOBS = 1..{jobs};
TASKS = 1..{tasks};
MACHINES = 1..{machines};
SPEEDSCALING = 1..{speedscaling};

time = array3d(JOBS,TASKS,SPEEDSCALING,{time});
energy = array3d(JOBS,TASKS,SPEEDSCALING,{energy});
precedence = array2d(JOBS,TASKS,{precedence});