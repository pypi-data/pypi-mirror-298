hydra:
  run:
    dir: ./.wisio/${now:%Y-%m-%d}-${now:%H-%M-%S}

defaults:
  - base_config
  - analysis: default
  - checkpoint: disabled
  - cluster: local
  - output: console
  - _self_

debug: false
verbose: false
