[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 200
exclude = [".eggs", ".git", ".venv", ".vscode", "*_pb2.py"]

[tool.ruff.lint]
select = ["E", "F", "W", "UP", "TID252"]
preview = true
