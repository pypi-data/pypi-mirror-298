[project]
name = "amarillo-grfs-generator"
version = "2.0.1"
description = "FastAPI app providing GRFS generation for Amarillo carpools"
readme = "README.md"
license = {file = "LICENSE"}
dependencies = [
    "amarillo",
    "amarillo-stops",
    "schedule",
    "watchdog",
]

[tool.setuptools.packages]
find = {}
